#!/bin/bash

heroku container:login
docker login --username=_ --password="$HEROKU_API_KEY" registry.heroku.com
docker build -t registry.heroku.com/tiltbottest/web .
heroku container:push web -a tiltbottest
heroku container:release web -a tiltbottest
